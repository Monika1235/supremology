2. RentalApp (User App)

This is a separate mobile application for the customer/end user.

Capabilities:

User logs in using Firebase Auth

App fetches the violations using .whereEqualTo("userID")

Shows all the violations in RecyclerView

Maybe shows penalty, dates, totals, etc.

This is the data consumer.
